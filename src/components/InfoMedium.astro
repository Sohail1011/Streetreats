---
const { urlImage, name, paragraph } = Astro.props;
---

<div class="flex flex-col items-start gap-4">
  <img class="h-14" src={urlImage} alt="Product" />
  <h4 class="font-semibold text-xl">{name}</h4>
  <p class="text-left text-sm">{paragraph}</p>
</div>
